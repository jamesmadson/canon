---
interface Props {
  rating: number;
}

const { rating } = Astro.props;
---

<span class="text-[11px] leading-none font-medium tracking-[0.15em] text-ink-soft" aria-hidden="true">
  {'●'.repeat(rating)}{'○'.repeat(5 - rating)}
</span>
